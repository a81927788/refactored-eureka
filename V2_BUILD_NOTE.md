# Build note

This pull request updates source files only.

Before using GitHub Pages from `/docs`, rebuild locally or in GitHub Actions:

```bash
npm install
npm run build
```

Then commit the generated `docs/` output if the repository continues to deploy GitHub Pages from the `/docs` folder.
